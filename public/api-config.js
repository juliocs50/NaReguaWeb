/**
 * Se o proxy /api no Netlify der erro, cole aqui a URL da função `naReguaWebApi`
 * (Firebase Console → Functions → URL). Exemplo:
 * window.NA_REGUA_API_BASE = "https://southamerica-east1-MEU_PROJETO.cloudfunctions.net/naReguaWebApi";
 */
window.NA_REGUA_API_BASE = window.NA_REGUA_API_BASE || "";
